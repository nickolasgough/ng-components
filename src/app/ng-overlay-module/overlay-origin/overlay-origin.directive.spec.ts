import { OverlayOriginDirective } from './overlay-origin.directive';

describe('OverlayOriginDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayOriginDirective();
    expect(directive).toBeTruthy();
  });
});
